[project]
name = "logographic-chat"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "textual>=0.80",
    "httpx>=0.27",
    "websockets>=13.0",
    "click>=8.1",
]

[project.scripts]
logographic-chat = "logographic_chat.cli:main"
