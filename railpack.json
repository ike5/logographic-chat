{
  "$schema": "https://schema.railpack.com",
  "buildAptPackages": ["git"],
  "deploy": {
    "startCommand": "cd backend && pip3 install -r requirements.txt && daphne -b 0.0.0.0 -p $PORT logographic_chat.asgi:application"
  }
}
